# ============================================================
# OpenWrt 官方 24.10 稳定版编译
# 方案三：精确拉取，最小 污染
# 核心：CUPS + SSR-Plus + ksmbd + 中文界面
# ============================================================
name: Build Official OpenWrt

on:
  workflow_dispatch:

env:
  UPLOAD_BIN_DIR: false
  UPLOAD_FIRMWARE: true
  UPLOAD_RELEASE: true

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 360

    steps:
    - name: Checkout
      uses: actions/checkout@main

    - name: Initialization environment
      env:
        DEBIAN_FRONTEND: noninteractive
      run: |
        sudo rm -rf /etc/apt/sources.list.d/* /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache/CodeQL
        sudo docker image prune --all --force
        sudo -E apt-get -qq update
        sudo -E apt-get -qq install build-essential clang flex bison g++ gawk gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev python3 python3-pip python3-ply python3-setuptools rsync swig unzip zlib1g-dev file wget cpio rsync unzip file libelf-dev libfuse-dev libpam0g-dev libzstd-dev cups libcups2-dev
        sudo -E apt-get -qq clean
        sudo timedatectl set-timezone Asia/Shanghai

    - name: Clone OpenWrt 24.10
      run: |
        git clone https://github.com/openwrt/openwrt -b openwrt-24.10 openwrt

    - name: Load custom feeds and config
      run: |
        [ -f config.txt ] && cp config.txt openwrt/.config
        [ -f diy-part1.sh ] && cp diy-part1.sh openwrt/
        [ -f diy-part2.sh ] && cp diy-part2.sh openwrt/
        sed -i 's/\r$//' openwrt/diy-part1.sh openwrt/diy-part2.sh 2>/dev/null || true
        chmod +x openwrt/diy-part1.sh openwrt/diy-part2.sh 2>/dev/null || true
        cd openwrt
        ./diy-part1.sh
        echo "--- feeds.conf 内容 ---"
        cat feeds.conf

    - name: Cache OpenWrt source packages (dl)
      uses: actions/cache@v4
      with:
        path: openwrt/dl
        key: openwrt-dl-${{ hashFiles('diy-part1.sh') }}
        restore-keys: openwrt-dl-

    - name: Cache OpenWrt build artifacts
      uses: actions/cache@v4
      with:
        path: |
          openwrt/staging_dir
          openwrt/build_dir
        key: openwrt-build-${{ hashFiles('config.txt') }}
        restore-keys: openwrt-build-

    - name: Update feeds
      run: cd openwrt && ./scripts/feeds update -a

    - name: Install feeds - official only
      run: |
        cd openwrt
        ./scripts/feeds install -a -p packages
        ./scripts/feeds install -a -p luci

    - name: Install feeds - third party (precise)
      run: |
        cd openwrt

        # ===== smpackage 源 =====
        ./scripts/feeds install -p smpackage cups
        ./scripts/feeds install -p smpackage libcupsimage
        ./scripts/feeds install -p smpackage cups-filters
        ./scripts/feeds install -p smpackage luci-app-cupsd
        ./scripts/feeds install -p smpackage luci-app-ssr-plus
        ./scripts/feeds install -p smpackage luci-i18n-ssr-plus-zh-cn
        ./scripts/feeds install -p smpackage xray-core
        ./scripts/feeds install -p smpackage v2ray-geodata
        ./scripts/feeds install -p smpackage v2ray-geosite
        ./scripts/feeds install -p smpackage vlmcsd
        ./scripts/feeds install -p smpackage luci-app-vlmcsd
        ./scripts/feeds install -p smpackage autoshare-ksmbd
        ./scripts/feeds install -p smpackage luci-app-ksmbd
        ./scripts/feeds install -p smpackage luci-i18n-ksmbd-zh-cn

        # ===== printing 源（tobiaswaldvogel）=====
        ./scripts/feeds install -p printing ghostscript
        ./scripts/feeds install -p printing gutenprint
        ./scripts/feeds install -p printing cups-bjnp
        ./scripts/feeds install -p printing lcms2

        # ===== hplipfeed 源（woniuzfb）=====
        ./scripts/feeds install -p hplipfeed hplip

        # ===== immortalwrt 源 =====
        ./scripts/feeds install -p immortalwrt luci-app-autoreboot

        # ===== 官方 packages 源 =====
        ./scripts/feeds install -p packages luci-app-adblock
        ./scripts/feeds install -p packages luci-app-nlbwmon
        ./scripts/feeds install -p packages luci-app-commands
        ./scripts/feeds install -p packages luci-app-watchcat

    - name: Run diy-part2.sh
      run: |
        cd openwrt
        ./diy-part2.sh

    - name: Remove conflicting packages
      run: |
        cd openwrt
        rm -rf feeds/smpackage/{base-files,dnsmasq,firewall*,fullconenat,libnftnl,nftables,ppp,opkg,ucl,upx,vsftpd*,miniupnpd-iptables,wireless-regdb,curl,dbus,openssl} 2>/dev/null || true
        rm -rf feeds/immortalwrt/{base-files,dnsmasq,firewall*,fullconenat,libnftnl,nftables,ppp,opkg,ucl,upx,curl,dbus,openssl,cups,cups-filters,libcups,libcupsimage} 2>/dev/null || true
        rm -rf feeds/packages/net/onionshare-cli 2>/dev/null || true
        rm -rf feeds/smpackage/webd 2>/dev/null || true
        find . -name "onionshare-cli" -type d -exec rm -rf {} \; 2>/dev/null || true
        rm -rf feeds/smpackage/samba4* 2>/dev/null || true
        rm -rf feeds/*/net/samba4 2>/dev/null || true
        find . -name "samba4" -type d -exec rm -rf {} \; 2>/dev/null || true
        for idx in feeds/*.index; do
          [ -f "$idx" ] && sed -i '/onionshare-cli/d' "$idx" || true
        done
        DBUS_MK="feeds/packages/utils/dbus/Makefile"
        if [ -f "$DBUS_MK" ] && ! grep -q "Package/dbus-libs" "$DBUS_MK"; then
          sed -i '/^define Package\/dbus$/,/^endef$/{ /DEPENDS:/ s/$/ +dbus-libs/ }' "$DBUS_MK"
          printf '\ndefine Package/dbus-libs\n  SECTION:=libs\n  CATEGORY:=Libraries\n  DEPENDS:=+libexpat\n  TITLE:=D-Bus library\nendef\n\ndefine Package/dbus-libs/install\n\t$(INSTALL_DIR) $(1)/usr/lib\n\t$(CP) $(PKG_INSTALL_DIR)/usr/lib/libdbus-1.so* $(1)/usr/lib/\nendef\n\n$(eval $(call BuildPackage,dbus-libs))\n' >> "$DBUS_MK"
          echo "  ✅ dbus-libs 修复已应用"
        fi
        rm -rf tmp/
        echo "✅ 冲突包已清除"

    - name: Run make defconfig
      run: cd openwrt && make defconfig

    - name: Force enable key packages
      run: |
        cd openwrt
        enable_pkg() {
          local pattern="$1"
          if grep -q "^# CONFIG_PACKAGE_${pattern} is not set" .config; then
            sed -i "s/^# CONFIG_PACKAGE_${pattern} is not set/CONFIG_PACKAGE_${pattern}=y/" .config
            echo "  强制启用: $pattern"
          elif grep -q "^CONFIG_PACKAGE_${pattern}=y" .config; then
            echo "  已启用: $pattern"
          else
            echo "CONFIG_PACKAGE_${pattern}=y" >> .config
            echo "  追加启用: $pattern"
          fi
        }
        
        echo "【强制启用 LuCI 核心依赖】"
        enable_pkg "luci-base"
        enable_pkg "luci-mod-admin-full"
        enable_pkg "luci-mod-network"
        enable_pkg "luci-mod-status"
        enable_pkg "luci-mod-system"
        enable_pkg "rpcd"
        enable_pkg "rpcd-mod-file"
        enable_pkg "rpcd-mod-iwinfo"
        enable_pkg "rpcd-mod-rpcsys"
        enable_pkg "rpcd-mod-rrdns"
        enable_pkg "uhttpd"
        enable_pkg "uhttpd-mod-ubus"
        enable_pkg "cgi-io"
        enable_pkg "ucode"
        enable_pkg "ucode-mod-fs"
        enable_pkg "ucode-mod-math"
        enable_pkg "ucode-mod-nl80211"
        enable_pkg "ucode-mod-resolv"
        enable_pkg "ucode-mod-ubus"
        enable_pkg "ucode-mod-uci"
        enable_pkg "luci-theme-bootstrap"
        
        echo ""
        echo "【强制启用中文语言包】"
        enable_pkg "luci-i18n-base-zh-cn"
        enable_pkg "luci-i18n-opkg-zh-cn"
        enable_pkg "luci-i18n-firewall-zh-cn"
        enable_pkg "luci-i18n-attendedsysupgrade-zh-cn"
        enable_pkg "luci-i18n-wireguard-zh-cn"
        enable_pkg "luci-i18n-upnp-zh-cn"
        enable_pkg "luci-i18n-ddns-zh-cn"
        enable_pkg "luci-i18n-sqm-zh-cn"
        enable_pkg "luci-i18n-wol-zh-cn"
        enable_pkg "luci-i18n-nft-qos-zh-cn"
        enable_pkg "luci-i18n-ttyd-zh-cn"
        enable_pkg "luci-i18n-ssr-plus-zh-cn"
        enable_pkg "luci-i18n-cupsd-zh-cn"
        enable_pkg "luci-i18n-adblock-zh-cn"
        enable_pkg "luci-i18n-nlbwmon-zh-cn"
        enable_pkg "luci-i18n-commands-zh-cn"
        enable_pkg "luci-i18n-watchcat-zh-cn"
        
        echo ""
        echo "【强制启用打印机驱动】"
        enable_pkg "hplip"
        enable_pkg "gutenprint"
        
        echo ""
        echo "【强制启用基础依赖】"
        enable_pkg "liblcms2"
        enable_pkg "zlib"
        enable_pkg "libcups"
        enable_pkg "libcupsimage"
        enable_pkg "libpng"
        enable_pkg "libjpeg"
        enable_pkg "libstdcpp"
        enable_pkg "libusb-1-0"
        enable_pkg "libexpat"
        enable_pkg "dbus"
        enable_pkg "dbus-libs"
        enable_pkg "libdaemon"
        enable_pkg "libiwinfo"
        enable_pkg "kmod-usb-printer"
        
        echo ""
        echo "【强制启用核心功能包】"
        enable_pkg "cups"
        enable_pkg "cups-filters"
        enable_pkg "cups-bjnp"
        enable_pkg "ghostscript"
        enable_pkg "avahi"
        enable_pkg "avahi-dbus-daemon"
        enable_pkg "luci-app-cupsd"
        enable_pkg "ksmbd-server"
        enable_pkg "ksmbd-utils"
        enable_pkg "luci-app-ksmbd"
        enable_pkg "luci-app-ssr-plus"
        enable_pkg "luci-app-sqm"
        enable_pkg "luci-app-upnp"
        enable_pkg "luci-app-ddns"
        enable_pkg "luci-app-wol"
        enable_pkg "luci-proto-wireguard"
        enable_pkg "vlmcsd"
        enable_pkg "luci-app-vlmcsd"
        enable_pkg "curl"
        enable_pkg "libcurl"
        enable_pkg "adblock"
        enable_pkg "luci-app-adblock"
        enable_pkg "nlbwmon"
        enable_pkg "luci-app-nlbwmon"
        enable_pkg "luci-app-commands"
        enable_pkg "watchcat"
        enable_pkg "luci-app-watchcat"
        enable_pkg "luci-app-autoreboot"
        
        sed -i 's/^CONFIG_PACKAGE_mosdns=y/# CONFIG_PACKAGE_mosdns is not set/' .config
        sed -i 's/^CONFIG_PACKAGE_luci-app-ssr-plus_INCLUDE_MosDNS=y/# CONFIG_PACKAGE_luci-app-ssr-plus_INCLUDE_MosDNS is not set/' .config
        sed -i 's/CONFIG_GRUB_TIMEOUT=.*/CONFIG_GRUB_TIMEOUT=0/' .config
        echo "CONFIG_GRUB_TIMEOUT=0" >> .config
        echo "==========================="

    - name: Re-run defconfig to resolve dependencies
      run: cd openwrt && make defconfig && echo "✅ defconfig 完成"

    - name: Download packages
      run: cd openwrt && make download -j8 || make download -j1 V=s

    - name: Compile OpenWRT firmware
      id: compile
      run: |
        cd openwrt
        echo -e "$(nproc) thread compile"
        make -j$(nproc) 2>&1 | tee build.log
        if [ ${PIPESTATUS[0]} -eq 0 ]; then
            echo "Build success with -j$(nproc)"
        else
            echo "Build failed, extracting errors..."
            grep -E "error:|undefined reference|cannot find -l|fatal error" build.log | tail -30
            echo "Retrying with -j1 V=s for detailed error..."
            make -j1 V=s 2>&1 | tee -a build.log
            if [ ${PIPESTATUS[0]} -ne 0 ]; then
                echo "========== BUILD FAILED =========="
                grep -E "error:|undefined reference|cannot find -l|fatal error" build.log | tail -50
                exit 1
            fi
        fi
        
        echo "status=success" >> $GITHUB_OUTPUT
        grep '^CONFIG_TARGET.*DEVICE.*=y' .config | sed -r 's/.*DEVICE_(.*)=y/\1/' > DEVICE_NAME
        [ -s DEVICE_NAME ] && echo "DEVICE_NAME=_$(cat DEVICE_NAME)" >> $GITHUB_ENV
        echo "FILE_DATE=_$(date +"%Y%m%d%H%M")" >> $GITHUB_ENV

    - name: Organize files
      id: organize
      if: env.UPLOAD_FIRMWARE == 'true' && !cancelled()
      run: |
        cd openwrt/bin/targets/*/*
        rm -rf packages
        echo "FIRMWARE=$PWD" >> $GITHUB_ENV
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload firmware to Artifacts
      uses: actions/upload-artifact@main
      if: steps.organize.outputs.status == 'success' && !cancelled()
      with:
        name: OpenWrt_Official_firmware${{ env.DEVICE_NAME }}${{ env.FILE_DATE }}
        path: ${{ env.FIRMWARE }}

    - name: Generate release tag
      id: tag
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      run: |
        echo "release_tag=$(date +"%Y.%m.%d-%H%M")" >> $GITHUB_OUTPUT
        touch release.txt
        echo "## 🚀 OpenWrt 24.10 固件" >> release.txt
        echo "" >> release.txt
        echo "### 📦 包含功能" >> release.txt
        echo "- ✅ LuCI 中文界面" >> release.txt
        echo "- ✅ CUPS 打印系统 + AirPrint 支持" >> release.txt
        echo "- ✅ ksmbd 文件共享" >> release.txt
        echo "- ✅ SSR-Plus 代理" >> release.txt
        echo "- ✅ 网络工具 (UPnP, DDNS, WoL, SQM)" >> release.txt
        echo "- ✅ 广告屏蔽 (adblock)" >> release.txt
        echo "- ✅ 网络流量统计 (nlbwmon)" >> release.txt
        echo "- ✅ Web 命令执行 (commands)" >> release.txt
        echo "- ✅ 断网自动重启 (watchcat)" >> release.txt
        echo "- ✅ 定时重启 (autoreboot)" >> release.txt
        echo "" >> release.txt
        echo "### 🖨️ 打印机驱动" >> release.txt
        echo "- HP (hplip)" >> release.txt
        echo "- Gutenprint" >> release.txt
        echo "" >> release.txt
        echo "**编译时间**: $(date '+%Y-%m-%d %H:%M:%S')" >> release.txt
        echo "status=success" >> $GITHUB_OUTPUT

    - name: Upload firmware to Release
      uses: softprops/action-gh-release@master
      if: steps.tag.outputs.status == 'success' && !cancelled()
      with:
        token: ${{ secrets.RELEASES_TOKEN }}
        tag_name: ${{ steps.tag.outputs.release_tag }}
        name: "OpenWrt 24.10 - ${{ steps.tag.outputs.release_tag }}"
        body_path: release.txt
        files: ${{ env.FIRMWARE }}/*

    - name: Remove old Releases
      uses: dev-drprasad/delete-older-releases@master
      if: env.UPLOAD_RELEASE == 'true' && !cancelled()
      with:
        keep_latest: 3
        delete_tags: true
      env:
        GITHUB_TOKEN: ${{ secrets.RELEASES_TOKEN }}
